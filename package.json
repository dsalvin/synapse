{
  "name": "synapse-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/client",
    "apps/server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/client & npm run dev --workspace=apps/server"
  }
}